export {
    _createWindow as createMainWindow,
    _showWindow as showMainWindow,
    _getWindow as getMainWindow,
} from "./main-window";

export {
    _createWindow as createLyricWindow,
    _closeWindow as closeLyricWindow,
    _getWindow as getLyricWindow,
} from "./lyric-window";

export {
    _closeWindow as closeMinimodeWindow,
    _getWindow as getMinimodeWindow,
    _showWindow as showMinimodeWindow,
} from "./minimode-window";
